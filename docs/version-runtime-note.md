# Runtime Version Note

Current product version: 0.3.13

The public README and VERSION ledger are synced to 0.3.13.

The runtime FastAPI version string is still hard-coded in core/app/main.py and should be moved to a single version constant in a later small patch.
